# Cylist — Implementation Plan

Approved mock: https://claude.ai/code/artifact/27e0344e-ef48-4e2a-a592-5e5c53c1f935

## 1. Goals and constraints

- Personal project/task manager: Projects → { Kanban board, Files, Vault, People }.
- Stack: React (Vite + TypeScript) · FastAPI (Python 3.12) · PostgreSQL 16.
- **API-first.** The web UI is one client. An MCP server and a CLI come later and must need *no* backend changes — so every capability is a REST endpoint, auth is token-based, and no logic lives only in React.
- Single owner (you) plus non-human agents. No multi-tenant auth in v1.
- Files stored on the server's disk; vault secrets encrypted at rest.

## 2. Architecture

```
┌───────────┐   ┌───────────┐   ┌───────────┐
│ React SPA │   │ MCP server│   │   CLI     │   (later: thin clients)
└─────┬─────┘   └─────┬─────┘   └─────┬─────┘
      └───────────────┼───────────────┘   HTTPS + Bearer token
                ┌─────▼─────┐
                │  FastAPI  │  /api/v1  (OpenAPI schema is the contract)
                │  service  │
                └──┬─────┬──┘
        ┌──────────┘     └──────────┐
   ┌────▼─────┐               ┌─────▼─────┐
   │PostgreSQL│               │ ./data/   │  uploaded files (sha256-named blobs)
   └──────────┘               └───────────┘
```

Why this matters for MCP/CLI:
- The **OpenAPI schema** generated by FastAPI is the single contract. The CLI can be generated from it; the MCP server maps tools 1:1 onto endpoints.
- **Pydantic schemas live in a shared package** (`cylist-core`) so the MCP server and CLI import the same models rather than re-declaring them.
- **API tokens** (`Authorization: Bearer cyl_…`) are first-class from day one, with scopes (`read`, `write`, `vault:read`, `vault:reveal`). The browser uses a session cookie that maps to the same principal model. An agent gets a token with the least scope it needs — e.g. an agent that moves cards never gets `vault:reveal`.
- Every mutation is recorded in an **activity log** (`who`, `what`, `when`, `via: web|api`) so agent actions are auditable.

## 3. Repository layout (monorepo)

```
cylist/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI app, routers, CORS, static serving
│   │   ├── config.py            # pydantic-settings (DB URL, DATA_DIR, VAULT_KEY…)
│   │   ├── db.py                # SQLAlchemy 2.0 engine/session
│   │   ├── auth/                # tokens, scopes, session cookie, dependencies
│   │   ├── models/              # SQLAlchemy ORM (one file per aggregate)
│   │   ├── schemas/             # Pydantic in/out models  ← shared with MCP/CLI later
│   │   ├── routers/             # projects, people, boards, tasks, files, vault, tokens, activity
│   │   ├── services/            # business rules (status change, column cap, encryption)
│   │   └── storage/             # local blob store (interface + disk impl)
│   ├── alembic/                 # migrations
│   ├── tests/                   # pytest + httpx, Postgres via testcontainers or a test DB
│   └── pyproject.toml           # uv-managed
├── frontend/
│   ├── src/
│   │   ├── api/                 # generated TS client from OpenAPI (openapi-typescript)
│   │   ├── routes/              # Home, Project, Board, Files, Vault, People
│   │   ├── components/          # Card, Modal, TaskCard, Column, Tree, PersonPicker…
│   │   ├── theme/               # tokens from the mock (CSS vars, light/dark)
│   │   └── main.tsx
│   ├── index.html
│   └── package.json             # Vite + React 18 + TS, TanStack Query + Router, dnd-kit
├── docker-compose.yml           # postgres + backend + frontend(dev) 
├── Makefile                     # make dev / test / migrate / seed
└── PLAN.md
```

Frontend libraries (deliberately few): TanStack Router + Query, `@dnd-kit/core` for drag-and-drop, Zod for form validation, plain CSS modules with the mock's tokens (no Tailwind — the design system is already written).

## 4. Data model (PostgreSQL)

All ids are UUIDv7; all tables have `created_at`, `updated_at`.

| Table | Key columns | Notes |
|---|---|---|
| `project` | `key` (unique, e.g. ATL), `name`, `description`, `color`, `archived_at` | |
| `person` | `name`, `kind` (`team`/`client`), `role`, `responsibilities`, `email`, `color` | **Global directory** — a client can appear in several projects |
| `project_member` | `project_id`, `person_id`, PK(both) | membership; assignee/tag pickers read from this |
| `board_column` | `project_id`, `name`, `description`, `position` | `CHECK` + service rule: 2 ≤ count ≤ 8; first column (`position=0`) is where new tasks land |
| `goal` | `project_id`, `number` (per-project sequence → ATL-G1), `name`, `description`, `colour`, `status` (`open`/`achieved`/`dropped`), `achieved_at`, `target_date`, `owner_id`→person | an epic. Unique name per project; progress is counted from its cards, never stored |
| `task` | `project_id`, `number` (per-project sequence → ATL-41), `column_id`, `position`, `title`, `description`, `type` (`feature`/`bug`/`chore`), `due_date`, `assignee_id`→person, `status` (`active`/`hold`/`blocked`), `goal_id` (nullable, `SET NULL`), `jira_ref`, `pr_ref` | required fields enforced in schema; `goal_id` only on top-level cards |
| `task_waiting_on` | `task_id`, `person_id` | people tagged on the *current* hold/block; cleared when status returns to active |
| `task_comment` | `task_id`, `author_id` (person, nullable for agents), `body`, `kind` (`comment`/`status_change`), `meta jsonb` (`{from,to,reason,tagged:[…]}`) | status changes are comments — one timeline |
| `folder` | `project_id`, `parent_id` (nullable), `name` | adjacency list; path built in API |
| `file_item` | `folder_id`, `kind` (`file`/`link`), `name`, `blob_id` nullable, `url` nullable, `source` (`upload`/`sharepoint`/`gdrive`/`other`), `size`, `mime`, `added_by` | |
| `blob` | `sha256` (unique), `size`, `mime`, `path` | content-addressed under `DATA_DIR/blobs/ab/cd/<sha>` — dedupes uploads |
| `vault_tree` | `project_id`, `name`, `position` | |
| `vault_node` | `tree_id`, `parent_id` nullable, `name`, `kind` (`branch`/`secret`), `position` | |
| `vault_secret` | `node_id` (1:1), `username`, `url`, `notes`, `secret_ciphertext bytea`, `key_version` | only the secret value is encrypted; username/url/notes are searchable |
| `api_token` | `name`, `token_hash`, `scopes text[]`, `last_used_at`, `revoked_at` | prefix `cyl_`, shown once |
| `activity` | `actor` (`token_id` or `web`), `verb`, `entity_type`, `entity_id`, `project_id`, `payload jsonb` | audit trail for agents |

Vault encryption: AES-256-GCM via `cryptography`, data key derived from `CYLIST_VAULT_KEY` (env/secret file), `key_version` column allows rotation. Reveal is a separate endpoint (`…/reveal`) requiring `vault:reveal`, and every reveal is logged to `activity`.

## 5. API surface (`/api/v1`)

Resource-oriented, JSON, cursor-less (data is small). `PATCH` for partial updates.

```
POST   /auth/login                 (password → session cookie, web only)
GET    /me
GET|POST        /tokens            POST returns the token once
DELETE          /tokens/{id}

GET|POST        /projects
GET|PATCH|DELETE/projects/{id}
GET             /projects/{id}/summary        counts for the hub cards

GET|POST        /people                       global directory
GET|PATCH|DELETE/people/{id}
GET|PUT         /projects/{id}/members        replace membership list

GET|POST        /projects/{id}/columns        POST 409 if 8 already
PATCH|DELETE    /columns/{id}                 DELETE 409 if tasks present or would drop below 2
PUT             /projects/{id}/columns/order

GET|POST        /projects/{id}/goals          ?open_only= leaves out settled ones
GET|PATCH|DELETE/goals/{ref}                  ATL-G1; PATCH status=achieved 422s over open cards

GET|POST        /projects/{id}/tasks          POST always lands in first column
GET|PATCH|DELETE/tasks/{id}
POST            /tasks/{id}/move              {column_id, position}
POST            /tasks/{id}/status            {status, reason?, waiting_on?: [person_id]}
                                              → 422 if status≠active and reason missing
GET|POST        /tasks/{id}/comments

GET|POST        /projects/{id}/folders
PATCH|DELETE    /folders/{id}
GET             /folders/{id}/children        subfolders + items
POST            /folders/{id}/upload          multipart → file_item
POST            /folders/{id}/links           {name, url, source}
GET             /items/{id}/download
PATCH|DELETE    /items/{id}

GET|POST        /projects/{id}/vault/trees
GET             /vault/trees/{id}             full tree (no secret values)
POST            /vault/nodes                  {tree_id, parent_id, name, kind, secret?}
PATCH|DELETE    /vault/nodes/{id}
POST            /vault/nodes/{id}/move
POST            /vault/nodes/{id}/reveal      scope vault:reveal, logged

GET             /projects/{id}/activity
```

Business rules live in `services/`, not routers, so MCP tools can call them directly if we ever embed the MCP server in-process.

## 6. Frontend

- Routes: `/` · `/p/:key` · `/p/:key/board` · `/p/:key/goals` · `/p/:key/goals/:ref` · `/p/:key/files/*` · `/p/:key/vault` · `/p/:key/people`.
- Port the mock's CSS tokens and components 1:1 (Figtree / Source Sans 3 / IBM Plex Mono, warm ground, yellow brand, green go-button, status tints).
- Board: `@dnd-kit` for cross-column drag; optimistic move via TanStack Query mutation; "+ Add a task" only under first column; column cap UI as in mock.
- Task dialog: status segmented control → reason textarea + "waiting on" person chips, both required-when-not-active, mirrored server-side.
- Files: tree + table, drag-drop upload, links inline.
- Vault: trees, branch/secret nodes, masked values with Reveal (calls `/reveal`).
- People: global directory page + per-project membership; pickers read members only.
- Types generated from OpenAPI (`openapi-typescript`) so the UI can't drift from the API.

## 7. Dev environment & ops

- `docker compose up`: Postgres 16 with a named volume; backend with `DATA_DIR=/data` volume; Vite dev server proxying `/api`.
- Production: single container serves the built SPA from FastAPI (`StaticFiles`) — one process, one port.
- Config via env: `DATABASE_URL`, `DATA_DIR`, `CYLIST_VAULT_KEY`, `CYLIST_PASSWORD_HASH`, `MAX_UPLOAD_MB`.
- Seed script reproduces the mock's Atlas/Hermes/Orbit data for dev.
- Backups: `pg_dump` + rsync of `DATA_DIR`; both needed to restore.

## 8. Build order

Each phase ends with something usable end-to-end.

| # | Phase | Deliverable |
|---|---|---|
| 0 | Scaffold | monorepo, compose, Alembic baseline, auth (password + tokens + scopes), activity log, health endpoint, CI running pytest + tsc |
| 1 | Projects + People | project CRUD, home grid, project hub with counts, global people directory + membership |
| 2 | Kanban | columns (2–8), tasks, drag-drop, task dialog, status change with reason + waiting-on tags, comments timeline |
| 3 | Files | folders, uploads to blob store, links, download, tree UI |
| 4 | Vault | trees/nodes, encrypted secrets, reveal with scope + audit |
| 5 | Polish | dark theme pass, keyboard access, seed data, backup script, README |
| 6 | Agents | `cylist` CLI over the HTTP API; MCP server exposing 14 tools, with `reveal_secret` registered only when the token carries `vault:reveal` |

**All six phases are built.** See the README for what exists and how to run it.

## 9. Decisions taken

1. **People are a global directory** with per-project membership (not per-project copies).
2. **Status-change reason is required** for On hold / Blocked, enforced by the API, stored as a comment.
3. **Tagging people** is available on status changes only in v1; @-mentions in free comments are a later add.
4. **Single user + API tokens with scopes**; no roles/teams login.
5. **Vault**: only the secret value is encrypted; reveal is a distinct, logged, scoped action.
6. **Uploads are content-addressed** (sha256) with a configurable size cap (default 200 MB).
7. **No Tailwind / component library** — the mock's design system is ported as CSS variables + modules.

## 10. Decisions taken during the build

These came up while implementing and are worth knowing:

8. **Every project owns a real root folder row.** Files sit at a project's root as the mock showed; `parent_id IS NULL` plus a partial unique index makes "one root per project" a schema fact. The root is named after its project and cannot be renamed or deleted.
9. **Tasks are created only in the first column**, and can then be moved anywhere.
10. **A task's number comes from a counter on the project**, taken under `SELECT … FOR UPDATE`, so a deleted `ATL-1` is never reissued.
11. **Only the vault's secret *value* is encrypted** (AES-256-GCM, fresh nonce per write, bound to its node with GCM associated data). Username, URL and notes stay searchable.
12. **Blobs are content-addressed by SHA-256**, so two uploads of identical bytes share one file on disk.
13. **`/activity` takes a project key**, like every other project-scoped path — added after the CLI and MCP server both had to resolve a key to an id first.
14. **A goal takes the card's left-hand rail.** The rail used to be the status — green, amber, red, grey. A card on a goal now wears the goal's colour there instead, and only a card on no goal falls back to the status colours. Status was already on the card three times over (the tint, the icon tile, the word on the pill); a goal had nowhere else on a board to be.
15. **A card belongs to at most one goal**, and a sub-task to none of its own — its card is what belongs to the goal. Both are check constraints rather than conventions: one colour on one rail, and one honest answer to "what is left on this goal".
16. **A goal's progress is never stored.** It is counted from its cards on every read. A percentage kept beside them is a number that can disagree with them.
17. **A goal cannot be marked achieved while a card on it is open** — the same shape as the rule that keeps a card out of the last column with a sub-task outstanding, and the refusal names what is holding it open. Dropping a goal carries no such rule.
