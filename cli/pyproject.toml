[project]
name = "cylist-cli"
version = "0.1.0"
description = "cylist — the Cylist command line, talking to the same HTTP API the web app uses."
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx>=0.28",
]

[project.scripts]
cylist = "cylist_cli.main:run"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.8",
    "mypy>=1.14",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cylist_cli"]

# --- Lint / format ---------------------------------------------------------
# Deliberately identical to backend/pyproject.toml: one house style across the
# monorepo, so moving between packages needs no re-learning.
[tool.ruff]
line-length = 100
target-version = "py312"
src = ["."]

[tool.ruff.lint]
select = [
    "E", "W",   # pycodestyle
    "F",        # pyflakes
    "I",        # isort
    "N",        # pep8-naming
    "UP",       # pyupgrade
    "B",        # bugbear
    "C4",       # comprehensions
    "SIM",      # simplify
    "RUF",      # ruff-specific
    "ASYNC",    # async correctness
    "S",        # bandit (security)
    "PTH",      # use pathlib
    "TID",      # tidy imports
]
ignore = [
    "S101",  # assert: used deliberately in tests
    "B008",  # function call in argument default
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S105", "S106"]  # hard-coded test credentials are fine

[tool.ruff.lint.isort]
known-first-party = ["cylist_cli"]

# --- Types -----------------------------------------------------------------
[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true

# --- Tests -----------------------------------------------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["error"]
